name: compile

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  compile:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - shell: bash
        run: |
          ./main.sh {{ secrets.URL_4D_19_272594_LTS }}
